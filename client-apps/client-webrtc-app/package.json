{
  "name": "client-webrtc",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc && node esbuild.config.mjs && cp src/index.html dist/",
    "start": "serve dist --ssl-cert ../../certs/localhost-fullchain.pem --ssl-key ../../certs/localhost-key.pem -p 443"
  },
  "devDependencies": {
    "esbuild": "^0.25.4",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.0.0"
  }
}