{
  "name": "mediasoup-demo",
  "private": true,
  "type": "module",
  "workspaces": [
    "libs/*",
    "rooms/*",
    "conf/*",
    "clients/*"
  ],
  "imports": {
    "#conf-server/*": "./conf/conf-server/src/*.js"
  },
  "scripts": {
    "build": "npm run --workspace=libs build && npm run --workspace=rooms build && npm run --workspace=conf build && npm run --workspace=clients build ",
    "package": "npm run --workspace=rooms/rooms-server package && npm run --workspace=conf/conf-server package",
    "deploy": "npm install --production && rm -rf deploy && mkdir -p deploy && npm run build && npm run package && npm run deploy-rooms && npm run deploy-conf && npm run deploy-client",
    "deploy-rooms": "mkdir -p deploy && cp -rf rooms/rooms-server/dist/. deploy/rooms-server",
    "deploy-conf": "mkdir -p deploy && cp -rf conf/conf-server/dist/. deploy/conf-server",
    "deploy-client": "mkdir -p deploy && cp -rf clients/conf-client-app/build/. deploy/conf-client-app",
    "build-libs": "npm run --workspace=libs build && npm run --workspace=rooms build && npm run --workspace=conf build",
    "start": "concurrently --kill-others-on-fail \"npm run start --workspace=rooms/rooms-server\" \"npm run start --workspace=conf/conf-server\" \"npm run start --workspace=client-apps/client-conf-react-app\"",
    "delete-node_modules": "find . -type d -name \"node_modules\" -exec rm -rf {} +",
    "delete-dist": "find . -type d -name \"dist\" -exec rm -rf {} +",
    "delete-tsbuildinfo": "find . -type f -name \"tsconfig.tsbuildinfo\" -delete ",
    "delete-package-lock": "find . -type f -name \"package-lock.json\" -delete ",
    "package-all": "npm rum build && node scripts/package.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "typescript": "^5.8.3"
  },
  "version": "",
  "dependencies": {
    "mitt": "^3.0.1"
  }
}